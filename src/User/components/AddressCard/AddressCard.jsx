import React from 'react'

const AddressCard = ({ address, username }) => {
  console.log(address, username);

  return (
    <div className=''>
      adc
    </div>
  )
}

export default AddressCard
